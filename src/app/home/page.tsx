import HomeScreen from '../../components/HomeScreen'

export const metadata = {
  title: 'FITLAB — Home',
  description: 'Your posture coach dashboard — pick an exercise and start training.',
}

export default function HomePage() {
  return <HomeScreen />
}
